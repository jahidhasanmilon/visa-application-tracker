// Used when an applicant doesn't have a custom roadmap yet — mirrors the
// stages the app used to hard-code before roadmaps became admin-editable.
export const DEFAULT_ROADMAP_LABELS = ['Applied', 'Submitted', 'Under Review', 'Approved'];
